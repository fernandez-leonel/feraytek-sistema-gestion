/**
 * ===============================================
 * Middleware: validate
 * Descripción: (Pendiente)
 * Verificará los campos de entrada para rutas POST/PUT.
 * ===============================================
 */

// VALIDACIONES en los campos de entrada (Fijate esta parte LEO, O MODIFICAR O ELIMINAR ESTE ARCHIVO)
function validate(fields) {
  return (req, res, next) => {
    // TODO: implementar validación de body
    next();
  };
}

module.exports = validate;
